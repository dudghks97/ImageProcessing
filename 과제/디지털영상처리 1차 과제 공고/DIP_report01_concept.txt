※ 미션번호에 따른 달성 내용을 자체 평가하는 표가 포함되어야함.
구성을 어떻게 할 것인가?
   1. 과제 설명
   2. 프로그램의 동작 개요
   3. 프로그램 구현
   4. 결과화면
   5. 결론 및 후기
   6. 평가표 답변

# 전체 동작 : 영상을 읽어 들여 이 영상의 밝기를 트랙바로 제어.

# 프로그램 동작 개요
1. 영상을 읽은 후, 트랙바 및 출력 윈도우를 생성한다.
2. 원본영상과 변환영상을 함께 출력하고, 트랙바를 통해 밝기를 제어한다.
3. cv.waitKey() 함수를 사용하여 사용자의 키 입력을 기다린다.
4. esc가 입력되면 프로그램을 종료하고, ‘s’ 가 입력되면 변환영상을 현재 소스파일의 경로에 저장한다. 
   두 경우가 아니라면, 영상에 텍스트를 삽입한다.


# 아래 동작은 gammaTrans가 아님
1 + 0.1 => 10% 밝게
1 + 3.0 => 300% 밝게
트랙바 입력이 n 이면 n / 10 으로 해서 계산

    # 영상 밝게
    s = 1 + (scale * 0.1)
    outC = (np.clip(255*(outC/255 * s), 0, 255)).astype('uint8')

outC = (np.clip(255*(outC/255 * s), 0, 255)).astype('uint8')
# cv.hconcat 으로 원본과 변환 영상 같이 출력
cv.hconcat([img, out])

# 트랙바를 통한 호출
트랙바는 하나만 사용 0~30 범위

    # 영상에 글자 삽입
    org = (50, 50)
    cv.putText(imgC, "Original", org, 0, 1, (0, 0, 255), 2)
    cv.putText(outC, "Brighter", org, 0, 1, (0, 0, 255), 2)

# 문제점 및 해결
1. copy 함수를 통해 영상의 복사본을 사용하지 않으면 원본 영상의 Original 과 변환 영상의 Brighter 가 겹쳐서 출력되는 문제 => 해결

2. 영상 저장 시 글자가 같이 저장되는 문제 -> 변환 영상에 글자를 넣기 전에 따로 복사본을 생성하고, 그 영상을 저장하였음
   ㄴ 코드가 복잡해보인다는 판단 => s 와 esc 미입력시, 텍스트를 삽입하도록 변경.

3. 변환에서 출력까지 일련의 동작을 콜백함수와 기타 함수 제작을 통해 하려 하였으나, 값은 변하지만 트랙바의 수치값은 그대로 유지되는
   문제 등의 여러 문제가 발생하였음. => 강의 자료의 예제 프로그램들을 참조하여 비슷한 방식으로 작성하였음